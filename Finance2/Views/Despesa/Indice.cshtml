@model IEnumerable<Finance.Models.Despesa>

@{
    ViewBag.Title = "Indice";
}

<h2>Indice</h2>

<p>
    @Html.ActionLink("Criar Novo", "Criar", new { }, new { @class = "button tiny primary" })
</p>
<table>
    <thead>
        <tr>
            <th>
                Descricao
            </th>
            <th>
                Valor
            </th>
            @*<th>
                DespesaCategoria
            </th>
            <th></th>*@
        </tr>
    </thead>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @item.Descricao
            </td>
            <td>
                @String.Format("{0:N2}", item.Valor)
            </td>
            @*<td>
                @Html.DisplayTextFor(_ => item.DespesaCategoria.Nome).ToString()
            </td>*@

            @*<td>
                @Html.ActionLink("Editar", "Editar", new { id = item.DespesaId }, new { @class = "button tiny primary" })
                @Html.ActionLink("Detalhes", "Detalhes", new { id = item.DespesaId }, new { @class = "button tiny secondary" })
                @Html.ActionLink("Excluir", "Excluir", new { id = item.DespesaId }, new { @class = "button tiny alert" })
            </td>*@
        </tr>
    }

</table>


